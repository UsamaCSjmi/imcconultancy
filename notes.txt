git init

-------Cloning git repo to local computer starts------
git clone git@github.com:UsamaCSjmi/imcconultancy.git
-------Cloning git repo to local computer ends------

git branch -M main
git add .
git commit -m "-----Message------"
git remote -v

------if remote url is not there then -----
git remote add origin git@github.com:UsamaCSjmi/imcconultancy.git
------end if--------------

----Push Code to remote repo starts---------
git push -u origin main
----Push Code to remote repo ends---------

-----Changing current working branch-------------
git branch danish
git switch danish
git branch 
--------After these commands the branch would changed to *danish(in green)---------

logos in slider
banner spelling
ggogle partner in awards
facebook partner in awards
clutch partner in awards
logo in Png
images from freepics
twitter to X icon
gapping in mobile
awards section center alignment in mobile